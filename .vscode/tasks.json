{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "command": "yarn",
            "args": ["build"],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$tsc"],
            "label": "yarn: build",
            "detail": "yarn build"
        }
    ]
}
